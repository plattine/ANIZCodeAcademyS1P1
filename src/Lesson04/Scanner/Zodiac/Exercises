Exercise: Rebuild the Code from Comments

You are given a test class where the Java code has been removed, but all comments are still there.
Your task is to rewrite the missing Java code by carefully reading and understanding the comments.

Open the file ZodiacTest.java.
You will see many comments explaining what the program should do.
All executable code has been deleted.
Write the correct Java code below each comment.

The program must:
Ask the user for their birth day
Ask the user for their birth month
Create a ZodiacSign object
Call a method that returns the zodiac sign
Print the result to the user